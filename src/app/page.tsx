import { ScorePageClient } from '../components/ScorePageClient';

/** Server component shell; the interactive scoring UI is the client component. */
const Page = () => <ScorePageClient />;

export default Page;
